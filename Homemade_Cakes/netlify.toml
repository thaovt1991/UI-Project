[build]
  command = "npm run build"
  publish = "dist/gojapan-ui/browser"

# Angular SPA — mọi route (vd. /courses) trả về index.html để client router xử lý
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
